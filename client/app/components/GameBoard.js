import React from 'react'
import _ from 'lodash'

class GameBoard extends React.Component {
    render() {
        <div>
            _({this.props.world}).forEach((tile) => {
        {tile}
        }})
        </div>
    }
}

export default GameBoard;